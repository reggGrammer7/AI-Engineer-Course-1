# Operator Overloading
base_liquid = ["water","milk"]
extra_liquid = ["lemon"]
# Using the + operator to combine two lists
combined_liquid = base_liquid + extra_liquid
print(f"liqquid after combining : {combined_liquid} \n")

strong_tea = ["black tea", "water"]*3
print(f"strong tea : {strong_tea}")
# from operator import itemgetter

"CINAMON"
raw_spice = bytearray(b"CINAMON")
print(f"Raw spice: {raw_spice}")


























